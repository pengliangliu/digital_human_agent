# 工程上手文档

本文面向第一次接手本项目的工程同学，目标是让你在 30 分钟内理解项目结构、跑起前后端、知道从哪里改功能，以及遇到常见问题时怎么定位。

## 1. 项目定位

本项目是“虚拟试衣魔镜”的智能数字人 Agent 原型。它负责把语音、摄像头跟踪、LLM 对话、TTS 语音回复和数字人动作串起来。

当前重点不是训练数字人模型，而是搭好运行时交互链路：

```text
浏览器魔镜前端
  -> 摄像头跟踪 / 麦克风采集 / 3D 数字人展示
  -> WebSocket
  -> FastAPI Agent 后端
  -> LLM / ASR / TTS / 记忆 / 工具调用
  -> AvatarAction 数字人动作事件
  -> 前端占位数字人或后续 CHGA runtime
```

项目中的 `智能数字人Agent技术方案.md` 是路线方案，本文是工程落地说明。

## 2. 目录速览

```text
dagital_human_agent/
├── app.py                         # ASGI 入口，可用于 uvicorn app:app
├── run.py                         # 本地启动后端入口
├── pyproject.toml                 # Python 项目依赖与打包配置
├── .env.example                   # 环境变量模板
├── configs/
│   ├── local.yaml                 # LLM / ASR / TTS / Avatar 配置
│   └── avatar_actions.yaml        # 数字人动作白名单配置
├── services/agent-api/
│   ├── app/
│   │   ├── main.py                # FastAPI app、WebSocket session 入口
│   │   ├── schemas.py             # ClientEvent / AvatarAction / AgentReply
│   │   ├── agent/                 # Agent 编排、提示词、工具、记忆
│   │   ├── adapters/              # LLM / ASR / TTS / Avatar 适配器
│   │   ├── runtime/               # 事件总线
│   │   └── vision/                # 后端视觉状态处理占位
│   └── tests/                     # 后端回归测试
├── apps/web/
│   ├── package.json               # 前端依赖和脚本
│   ├── vite.config.ts             # Vite 配置
│   └── src/
│       ├── App.tsx                # 前端主交互界面
│       ├── api/wsClient.ts        # WebSocket 客户端
│       ├── avatar/                # Three.js 数字人占位和动作执行
│       ├── vision/                # 摄像头跟踪 Hook
│       └── audio/                 # 麦克风采集 Hook
├── models/                        # 后续放数字人模型资产
├── data/                          # SQLite 记忆库等本地数据
└── docs/                          # 可放接口文档、后续设计文档
```

## 3. 环境准备

### 3.1 基础依赖

| 依赖 | 建议版本 | 用途 |
| --- | --- | --- |
| Python | 3.10 或更高 | FastAPI 后端 |
| Node.js | 18 或更高 | Vite + React 前端 |
| Chrome / Edge | 较新版本 | 摄像头、麦克风、WebGL |
| Git | 任意近版本 | 版本管理 |

### 3.2 重新创建 Python 虚拟环境

如果你看到类似下面的错误，说明当前 `venv` 指向的 Python 已失效：

```text
No Python at "...WindowsApps...python.exe"
```

处理方式是删除并重建虚拟环境：

```powershell
# 在项目根目录执行
Remove-Item -Recurse -Force .\venv
py -3.10 -m venv venv
.\venv\Scripts\python.exe -m pip install --upgrade pip
.\venv\Scripts\python.exe -m pip install -e ".[dev]"
```

如果机器上没有 `py -3.10`，先安装 Python 3.10+，再重试。

### 3.3 安装前端依赖

```powershell
cd apps\web
npm.cmd install
```

在 Windows PowerShell 中优先使用 `npm.cmd`。如果直接运行 `npm` 报 `npm.ps1` 执行策略错误，这是 PowerShell 脚本策略问题，不是项目代码问题。

## 4. 配置说明

### 4.1 环境变量

复制 `.env.example` 为 `.env`：

```powershell
Copy-Item .env.example .env
```

常用配置：

```env
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-xxxx
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

ASR_PROVIDER=cloud
TTS_PROVIDER=edge
AVATAR_API_URL=http://localhost:9000
```

没有 `OPENAI_API_KEY` 时，后端会退回 `MockLLMAdapter`，可以先跑通前后端链路。

### 4.2 本地 YAML 配置

`configs/local.yaml` 是运行时主配置，负责选择：

- `llm.provider`: `openai` / `ollama` / `anthropic`
- `asr.provider`: `cloud` / `local`
- `tts.provider`: `edge` / `openai` / `local`
- `avatar.adapter`: `null` / `http`

第一阶段建议使用：

```yaml
llm:
  provider: "openai"

tts:
  provider: "edge"

avatar:
  adapter: "null"
```

`avatar.adapter: null` 会把数字人动作打印到后端日志，同时也通过 WebSocket 发给前端占位数字人。

## 5. 启动项目

### 5.1 启动后端

在项目根目录：

```powershell
.\venv\Scripts\python.exe run.py
```

启动成功后访问：

```text
http://localhost:8000/health
http://localhost:8000/docs
```

也可以用 ASGI 入口启动：

```powershell
.\venv\Scripts\python.exe -m uvicorn app:app --host 0.0.0.0 --port 8000
```

### 5.2 启动前端

新开一个终端：

```powershell
cd apps\web
npm.cmd run dev
```

打开：

```text
http://localhost:5173
```

页面加载后会尝试连接：

```text
ws://localhost:8000/ws/session/{session_id}
```

如果后端没启动，前端会显示 WebSocket 连接失败。

## 6. 如何验证链路

### 6.1 最小手动验证

1. 启动后端。
2. 启动前端。
3. 打开 `http://localhost:5173`。
4. 允许浏览器访问摄像头。
5. 在文本输入框里输入一句话并发送。
6. 观察前端回复气泡、数字人表情/动作、后端日志。

不配置真实 LLM 时，也应看到 mock 回复。

### 6.2 前端构建验证

```powershell
cd apps\web
npm.cmd run build
```

当前构建会把 React 和 Three.js 分成独立 chunk，正常情况下不应再出现 500KB 大 chunk 警告。

### 6.3 后端测试

虚拟环境正常后，在项目根目录运行：

```powershell
.\venv\Scripts\python.exe -m pytest services\agent-api\tests -q
```

测试覆盖重点：

- 事件总线移除断开的 WebSocket 时不死锁。
- LLM tool call 能解析成 `ToolCall` schema。
- Agent 回复动作不提前发送 `speech_end`。

## 7. 关键数据协议

### 7.1 客户端事件

前端发给后端的统一格式：

```json
{
  "event": "audio.transcript",
  "payload": {
    "text": "帮我推荐一套通勤穿搭"
  }
}
```

常见事件：

| event | 来源 | 说明 |
| --- | --- | --- |
| `session.init` | 前端 | WebSocket 建连后初始化会话 |
| `session.config` | 前端 | 设置当前 session 配置，例如是否开启 TTS |
| `vision.state` | 前端 | 摄像头跟踪状态 |
| `audio.data` | 前端 | 麦克风音频分片，base64 |
| `audio.transcript` | 前端 | 已识别文本，或调试时手动输入 |

### 7.2 后端事件

后端发给前端的统一格式：

```json
{
  "event": "avatar.action",
  "payload": {
    "type": "look_at",
    "priority": "realtime",
    "duration_ms": 120,
    "payload": {
      "yaw": 8.0,
      "pitch": -2.0
    }
  }
}
```

常见事件：

| event | 说明 |
| --- | --- |
| `agent.reply` | Agent 文本回复、情绪、动作意图 |
| `avatar.action` | 数字人动作事件 |
| `tts.audio` | TTS 音频，base64 |
| `asr.result` | ASR 识别文本 |
| `tool.result` | 工具调用结果 |
| `error` | 后端处理错误 |

### 7.3 AvatarAction

核心动作 schema 在 `services/agent-api/app/schemas.py`：

```python
class AvatarAction(BaseModel):
    type: Literal[
        "look_at",
        "head_pose",
        "gesture",
        "expression",
        "speech_start",
        "speech_end",
        "lip_sync",
        "pose",
        "try_on",
        "load_avatar",
    ]
    priority: Literal["realtime", "normal", "blocking"] = "normal"
    duration_ms: int | None = None
    payload: dict[str, Any] = Field(default_factory=dict)
```

后续接 CHGA runtime 时，优先把真实动作接口适配到这个 schema，不要让业务层直接调用模型内部 API。

## 8. 后端代码怎么改

### 8.1 新增 LLM 供应商

改这里：

```text
services/agent-api/app/adapters/llm.py
services/agent-api/app/main.py
configs/local.yaml
.env.example
```

实现一个新的 `LLMAdapter` 子类，然后在 `_build_orchestrator()` 中按配置选择。

### 8.2 新增 Agent 工具

改这里：

```text
services/agent-api/app/agent/tools.py
services/agent-api/app/schemas.py
```

步骤：

1. 在 `AgentTools` 里新增业务方法。
2. 在 `handle_tool_call()` 里分发。
3. 在 `AVAILABLE_TOOLS` 里描述工具名、参数和用途。
4. 加测试，确认工具调用参数能解析。

### 8.3 调整数字人动作策略

改这里：

```text
services/agent-api/app/agent/behavior_planner.py
apps/web/src/avatar/avatarActions.ts
```

原则：

- 后端只生成动作意图。
- 前端或 Avatar runtime 负责具体动画执行。
- `speech_start` / `speech_end` 应跟随实际 TTS 播放，不要只跟随文本回复生成。

### 8.4 接入真实 Avatar Runtime

改这里：

```text
services/agent-api/app/adapters/avatar_action.py
configs/local.yaml
```

把配置切到：

```yaml
avatar:
  adapter: "http"
  http:
    base_url: "http://localhost:9000"
```

然后让 CHGA runtime 实现：

```text
POST /avatar/load
POST /avatar/action
POST /avatar/pose
```

第一版真实 runtime 不支持的动作可以忽略或记录日志，但接口不要变。

### 8.5 接入自建三维人体模型

如果只是想把 `.obj`、`.glb`、`.gltf`、`.fbx` 等常用三维人体模型放到魔镜前端展示，不需要先接 CHGA runtime。把模型放到：

```text
models/avatars/<avatar_id>/
```

示例：

```text
models/avatars/demo-human/
├── body.obj
├── body.mtl
├── texture.png
└── avatar.json
```

`avatar.json` 可选：

```json
{
  "name": "Demo Human",
  "model_file": "body.obj",
  "material_file": "body.mtl",
  "scale": 1.0,
  "position": [0, 0, 0],
  "rotation": [0, 0, 0],
  "head_node": ""
}
```

后端启动后会通过 `GET /api/avatars` 返回模型列表，并通过 `/models/...` 静态托管模型文件。前端会自动读取列表，在控制区提供模型选择下拉框；加载失败时会回退到内置占位数字人。

## 9. 前端代码怎么改

### 9.1 页面交互入口

主要看：

```text
apps/web/src/App.tsx
```

这里负责：

- 建立 WebSocket。
- 处理 `agent.reply` / `avatar.action` / `tts.audio`。
- 控制摄像头、麦克风、TTS 开关。
- 把动作传给 `AvatarScene`。

### 9.2 WebSocket 封装

主要看：

```text
apps/web/src/api/wsClient.ts
```

这里负责：

- 建连后发送 `session.init`。
- 暴露 `onOpen` / `onClose` / `onError`。
- 统一收发 `ClientEvent`。

### 9.3 数字人占位场景

主要看：

```text
apps/web/src/avatar/AvatarScene.tsx
apps/web/src/avatar/avatarActions.ts
```

当前是 Three.js 占位数字人，用于验证动作链路。后续可以替换为：

- 3DGS WebGL viewer。
- CHGA 实时渲染 canvas。
- Unity / Unreal 嵌入窗口。

### 9.4 摄像头跟踪

主要看：

```text
apps/web/src/vision/useFaceTracking.ts
```

当前实现是轻量皮肤色块启发式跟踪，用于原型验证。工程化版本建议替换为 MediaPipe Face Landmarker / Pose Landmarker。

## 10. 常见问题

### 10.1 PowerShell 提示无法加载 profile.ps1

这是用户级 PowerShell profile 签名问题。通常不影响命令执行，只是每次输出前会刷一段错误。可以暂时忽略。

### 10.2 npm.ps1 无法执行

用 `npm.cmd`：

```powershell
npm.cmd run dev
npm.cmd run build
```

### 10.3 后端启动提示 No Python at WindowsApps

当前虚拟环境失效，重建 `venv`。见本文 3.2。

### 10.4 前端显示 WebSocket 连接失败

检查：

- 后端是否启动。
- `http://localhost:8000/health` 是否可访问。
- 前端是否运行在 `localhost:5173`。
- 浏览器控制台是否有 WebSocket 错误。

### 10.5 TTS 没声音

检查：

- 浏览器是否允许播放声音。
- 前端 TTS 开关是否打开。
- 后端 `TTS_PROVIDER` 是否为 `edge`。
- 后端日志是否有 TTS 错误。

### 10.6 摄像头没有跟踪

检查：

- 浏览器是否授权摄像头。
- 页面右上角摄像头预览是否有画面。
- 前端“摄像头”开关是否打开。
- 光照是否过暗。

## 11. 建议开发流程

每次改动建议按这个顺序：

1. 明确要改的链路：前端、后端、还是协议。
2. 如果改后端行为，先补 `services/agent-api/tests` 下的测试。
3. 如果改前端行为，至少跑 `npm.cmd run build`。
4. 不要直接改 `README.md` 承载细节说明，优先更新本文或 `docs/` 下专题文档。
5. 不提交 `venv/`、`data/memory.db`、`apps/web/dist/`、`*.tsbuildinfo`。

## 12. 当前工程状态

已具备：

- FastAPI WebSocket 后端。
- React + Three.js 前端。
- 摄像头跟踪占位。
- 麦克风音频采集。
- LLM / ASR / TTS / Avatar adapter 抽象。
- SQLite 会话记忆。
- 数字人动作事件链路。
- 基础回归测试。

仍待工程化：

- 重建可用 Python 3.10+ 虚拟环境。
- 替换摄像头跟踪为 MediaPipe。
- 接入真实 CHGA / 3DGS runtime。
- 完成本地 ASR 和本地 LLM 部署配置。
- 为 WebSocket 端到端交互补集成测试。
- 为真实数字人动作接口补协议文档。
