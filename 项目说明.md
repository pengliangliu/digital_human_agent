# bp_team_collection

# 虚拟试衣智能系统 — 团队技术资产盘点 & 专利规划

> **用途**：收集团队所有技术模块信息，用于整合成完整产品链路 + 投资人
**填写方式**：每人找到自己的编号区域填写，可同时编辑
**截止时间**：____
> 

---

## 〇、系统全局信息（负责人填）

[business_plan_v2.md](attachment:3bd2cc07-4110-4eb1-bcd5-fe90c31310bc:business_plan_v2.md)

### 项目一句话描述

> 一个完善的虚拟试衣商业平台：包括用魔镜实时采集用户的信息，用3dgs实现人体重构和换装，实时的服装数字化（包括布料物理渲染），大数据服装推荐，前端3d展示和虚拟人交互
> 
> 
> ---
> 

### 我，们的系统要实现的完整链路（初步设想，后续会调整）

```
用户输入（照片/扫描）
  → 体型获取（?谁的模块）
  → 3D 数字人生成（?谁的模块）
  → 服装数字化 & 试穿（?谁的模块）
  → 智能推荐 / 交互（?谁的模块）
  → 前端展示 / 硬件展示（?谁的模块）
```

> 上面的链路中，哪些环节已经有人在做？哪些还是空白？
> 
> 
> 已覆盖：
> 空白：
> 

---

## 一、成员 1：_陈宇轩_（姓名）

### 1.1 基本信息

| 字段 | 内容 |
| --- | --- |
| 研究方向 | 3D Gaussian Splatting 可穿衣可驱动数字人 |
| 技术栈（语言/框架/硬件平台） | Python / PyTorch / CUDA / diff-gaussian-rasterization / Hydra 配置系统 / RTX 5090 (amax 服务器) |

### 1.2 你负责的模块

**模块名称**：CHGA — Clothed Human Gaussian Avatar 引擎

**一句话说清楚它是干什么的**：基于 Mesh 引导的 3D 高斯泼溅技术，从视频数据学习出可驱动、可换装、可实时渲染的 3D 数字人模型。

**在整个链路中处于什么位置**（勾选）：
- [ ] 输入端：采集用户数据（拍照/扫描/传感器）
- [x] 处理层：3D 重建 / 体型建模 / 数字人生成
- [x] 处理层：服装数字化 / 虚拟试穿 / 渲染
- [ ] 应用层：AI 推荐 / 智能交互 / LLM 对话
- [ ] 展示端：前端界面 / 硬件终端 / AR/VR 展示
- [ ] 基础设施：后端服务 / 部署 / 数据管理

### 1.3 输入和输出（非常重要——用来拼接模块间的关系）

**你的模块需要什么输入？**
> 例如：一张正面全身照 / SMPL 体型参数 / 服装的 2D 照片 / 点云数据 / …

- 输入 1：RGB 视频帧序列（单目或多视角同步）
- 输入 2：前景、body-part分割 Mask
- 输入 3：相机参数（OpenCV 格式：W2C 外参 `(4,4)` + K 内参 `(3,3)`）
- 输入 4：HMR姿态评估参数（sam-3d-body估计）

**你的模块产出什么输出？**
> 例如：3D 网格模型 / 体型尺寸数据 / 渲染图像 / 推荐列表 / …

- 输出 1：**可驱动的 3DGS 数字人模型**——给定任意 HMR 姿态，实时渲染出穿衣人体的 RGB 图像 + alpha 蒙版
- 输出 2：**标准 .ply 导出文件**——包含所有 Gaussian 属性（位置/旋转/缩放/球谐/不透明度），兼容任何 3DGS 查看器（SuperSplat、Luma、gsplat.js）
- …

**你的输出会给谁用？（你知道的话填，不确定写”不确定”）**

- .ply 文件 → 前端 WebGL 查看器（实时 3D 展示）
- 渲染图像 → 前端展示 / 推荐系统的视觉素材
- 多层 AvatarLayer → 虚拟试衣模块（换装逻辑）
- 体型参数（来自 SMPL shape）→ 尺码推荐模块

### 1.4 当前成果

**已经做出来的东西**（逐条列，越具体越好）：

1. **完整的 CHGA 训练管线**——从数据加载到渲染输出到 loss 反传的全流程，35+ Python 模块，8 层架构，支持 Hydra YAML 配置
2. **6 步可微变形管线**——非刚性网格变形 → 锚点插值 → TBN 局部偏移 → LBS 蒙皮 → 旋转组合 → 缩放组合，完整实现并可端到端训练
3. **三种体型模型集成**——SMPL (24 关节 / 6890 顶点) + SMPL-X (55 关节 / 10475 顶点) + MHR (Meta 127 关节 / 18439 顶点 / 45 shape 参数 / 72 表情系数)
4. **Mesh-Bound Gaussian 参数化**——每个 Gaussian 锚定在 mesh 面上，位置/旋转/缩放都在局部 TBN 坐标系下学习，支持服装层独立组合
5. **多层 AvatarLayer 架构**——Scene 类支持多层（body/garment/accessory），各层有独立 GaussianModel + AnchorInfo + MeshNonRigid，渲染时合并
6. **三级 SMPL 姿态优化**——per-frame 姿态嵌入 + GauHuman 风格 PoseRefiner MLP + ExAvatar 风格关节位置偏移
7. **MCMC 密度控制**——NeurIPS 2024 MCMC relocation + AbsGS 绝对梯度 + KL 散度候选选择 + mesh-aware 同步（clone/split/prune 时自动同步 AnchorInfo 和 EdgeInfo）
8. **标准 .ply 导出**——导出文件兼容 Khronos glTF KHR_gaussian_splatting 标准（2026 Q2），可直接在网页端查看
9. **18+ 损失函数**——L1 + LPIPS + Mask + AIAP 各向同性 + 网格拉普拉斯 + 静态偏移正则 + 蒙皮权重正则 + SMPL 姿态/shape/关键点监督
10. **完整训练基础设施**——Trainer + LossCompose + CheckpointManager + Hydra 配置 + 单元测试覆盖

**有没有能跑的 Demo？**
- [ ] 有，可以演示（附截图/视频链接：___）
- [x] 有原型但还不稳定
- [ ] 目前只有代码/算法，没有可视化的 Demo
- [ ] 还在开发中

**性能指标（有多少填多少，没有的跳过）**：

| 指标 | 数值 |
| --- | --- |
| 精度（如有） | PSNR 28.17 / 目标 31+ |
| 速度/帧率 | ~100ms/帧（40K Gaussians），优化后可达 60+ FPS |
| 所需设备/显卡 |  |
| 内存/显存占用 |  |
| 成本（硬件成本/云计算成本） |  |

### 1.5 技术亮点 & 差异化

**你用了什么核心技术/方法？**
3D Gaussian Splatting / Mesh-Guided 参数化 / Linear Blend Skinning (LBS) / 可学习网格变形（静态+动态 MLP）/ 球谐函数 (SH) 外观 / MCMC 密度控制 / CUDA 可微光栅化

**和别人做的最大区别是什么？**（一两句话就行，不需要很正式）

1. **vs 所有 2D Diffusion 试衣方案（Google/CatVTON/IDM-VTON）**：它们生成的是一张平面图，不能旋转、不能动画、不能换视角。CHGA 是真 3D 模型，原生支持 360° 旋转 + 任意姿态驱动 + 实时渲染。
2. **vs Gaussian Wardrobe (ETH 2026)**：Gaussian Wardrobe 是事后分解（先训练完整数字人，再拆服装层），我们的多层 AvatarLayer 架构是训练时就分层学习，理论上分离更干净、组合更灵活。（这是我们要超越它的核心点）
3. **vs 标准 3DGS 人体（GauHuman/3DGS-Avatar）**：它们的 Gaussian 是自由浮动的，我们的每个 Gaussian 锚定在 mesh 面上（face_idx + 重心坐标 + TBN 偏移），所以天然支持服装层拆分组合——这是标准 3DGS 做不到的。
4. **vs NeRF 方案**：NeRF 渲染一帧要几秒，3DGS 是毫秒级。

**这个技术有没有论文/开源项目作为基础？**（列出来，方便我确认技术路线）

- 3D Gaussian Splatting (Kerbl et al., SIGGRAPH 2023) — 基础渲染框架
- GauHuman (Hu et al., CVPR 2024) — PoseRefiner MLP 思路
- 3DGS-Avatar (Qian et al., CVPR 2024) — SkinningField MLP 思路
- ExAvatar (Zheng et al., ECCV 2024) — 关节位置偏移思路

### 1.6 专利抢占规划（重要）

> 我们需要想清楚哪些技术点值得申请专利来保护。不需要你现在就写专利，只需要回答：
> 

**你的模块中，哪个技术点是你自己原创的 / 有创新的？**

1. **Mesh-Bound Gaussian 参数化 + 多层组合机制**：每个 Gaussian 的位置/旋转/缩放都在所属 mesh 面的局部 TBN 坐标系下定义，不同服装层的 Gaussian 锚定在不同的 mesh 模板上，渲染时按层合并。这使得「换上衣不影响裤子」成为原生操作。
2. **三级层次化姿态优化管线**：将 per-frame 姿态嵌入 + 系统偏差校正 MLP + 关节位置偏移三种策略组合在一个可微管线中联合优化，单一现有工作只用其中一种。
3. **Mesh-Aware 密度控制**：在 Gaussian 的克隆/分裂/剪枝操作中自动同步 AnchorInfo（面绑定）和 EdgeInfo（K-NN 拓扑），保证密度控制不破坏 mesh 绑定关系。

**如果要写一个专利，标题大概会是什么？**（可以写多个）

1. 一种基于网格引导的三维高斯表征的可穿衣数字人建模方法及系统

**紧迫性：是否已有类似的公开工作？**
- [ ] 目前没看到类似的，可以从容准备
- [x] 有接近的公开工作，需要尽快提交
- [ ] 已经有非常接近的竞品/论文，必须立即行动

**你认为还有哪些相关方向值得我们先占坑？**（开放性思考）

### 1.7 资源 & 依赖

**你目前需要什么资源/支持才能推进？**
> 例如：更多 GPU / 特定数据集 / 某个队友的模块先跑通 / 硬件采购 / …

**你的模块是否依赖团队其他人的成果？依赖谁的什么？**

- 如果有做**数据采集/3D 扫描**的队友 → 我需要他们提供的多视角视频 + 相机标定参数
- 如果有做HMR **姿态估计/体型重建**的队友 → 我需要每帧的 HMR 参数作为输入
- 如果有做**前端/展示**的队友 → 我导出的 .ply 需要他们的 WebGL 查看器来呈现
- 如果有做**服装识别/推荐**的队友 → 我的多层 AvatarLayer 是他们做换装逻辑的基础

### 1.8 可提供的素材（勾选）

- [x]  技术架构图 / 流程图
- [x]  Demo 截图（高清，可放 PPT）
- [ ]  Demo 视频（30 秒 - 2 分钟）
- [x]  效果对比图（vs 现有方法 / before-after）
- [x]  实验数据表格
- [ ]  硬件实物照片
- [ ]  系统运行截屏

---

## 二、成员 2：_谢志宽__（姓名）

### 2.1 基本信息

| 字段 | 内容 |
| --- | --- |
| 研究方向 | 服装动力学 |
| 技术栈（语言/框架/硬件平台） | 微分方程数值解 |

### 2.2 你负责的模块

**模块名称**：服装动力学仿真

**一句话说清楚它是干什么的**：模拟人穿衣服的三维运动，产生的褶皱变形

**在整个链路中处于什么位置**（勾选）：处理层虚拟试穿
- [ ] 输入端：采集用户数据（拍照/扫描/传感器）
- [ ] 处理层：3D 重建 / 体型建模 / 数字人生成
- [√ ] 处理层：服装数字化 / 虚拟试穿 / 渲染
- [ ] 应用层：AI 推荐 / 智能交互 / LLM 对话
- [ ] 展示端：前端界面 / 硬件终端 / AR/VR 展示
- [ ] 基础设施：后端服务 / 部署 / 数据管理

### 2.3 输入和输出

**你的模块需要什么输入？**

- 输入 1：输入衣服的点云三维坐标
- 输入 2：衣服的力学参数，弯曲 剪切 刚度 内阻尼 外阻尼 人体点云三维坐标

**你的模块产出什么输出？**

- 输出 1：衣服的时间序列三维坐标
- 输出 2：

**你的输出会给谁用？**

输出给虚拟试穿中衣服的运动三维坐标用，理由是神经网络仅能学习见过的衣服褶皱，一旦人做出与训练中不同的新动作，衣服就是变得穿模不自然

### 2.4 当前成果

**已经做出来的东西**：
1.复现了隐式欧拉法
2.做了单自由度的时间序列神经网络精度是1e-12
3.

**有没有能跑的 Demo？**
- [ ] 有，可以演示（附截图/视频链接：___）
- [ ] 有原型但还不稳定
- [ ] 目前只有代码/算法，没有可视化的 Demo
- [ √] 还在开发中

**性能指标**：

| 指标 | 数值 |
| --- | --- |
| 精度（如有） | 1e-10 |
| 速度/帧率 | 100帧每秒 |
| 所需设备/显卡 | 5090 |
| 内存/显存占用 | 64G |
| 成本 | 低 |
| 网格数 | 上百万 |

### 2.5 技术亮点 & 差异化

**核心技术/方法**：我准备用神经网络代替微分方程数值解

**和别人的最大区别**：神经网络；微分方程数值解的最大缺陷是精度高则速度慢，速度快则精度低；神经网络能很好的解决这个问题，解算精度高，速度快。

**基于哪些论文/开源项目**：有限元法：(Position-based simulation of continuous materials Computers & Graphics 44 (2014) 1–10)

位置基于动力学:(Position based dynamics J. Vis. Commun. Image R. 18 (2007) 109–118)

隐式欧拉法：（Large Steps in Cloth Simulation（1998））

显式欧拉法：Deformation Constraints in a Mass-Spring Model to Describe Rigid Cloth Behavior（1995）

XPBD:(XPBD: Position-Based Simulation of Compliant Constrained Dynamics(2014))

HPBD:(Hierarchical Position Based Dynamics(2008))

### 2.6 专利抢占规划

**原创/创新的技术点**：使用神经网络替代微分方程

**专利标题设想**：使用神经网络替代微分方程

**紧迫性：**
- [ ] 没看到类似的，可以从容准备
- [ ] 有接近的公开工作，需要尽快
- [ √] 非常接近的竞品已存在，必须立即行动

**其他值得占坑的方向**：1.一种基于多层级神经网络的有限元仿真方法,其特征在于,包括以下步骤：S1.在布料仿真中,将待预测的布料建模成布料超弹性体模型,利用布料有限元方法将所述布料超弹性体模型划分若干个单元,对于每个单元构建动力学的能量表达式,所述能量表达式包含动能项、势能项和附加的外力做功项三个部分；S2.用牛顿迭代法和线搜索方法求解当所述能量表达式达到最小值时能量表达式的解,将能量表达式的解作为下一个时刻单元中每个点的位置并构建多层级神经网络的训练数据集；S3.预先获取一个细网格,由最远点采样法构建多层级粗细网格结构,多层级粗细网格结构由所述细网格和多层粗网格组成,每一层粗网格对应一个构建好的插值矩阵；S4.在所述训练数据集上训练多层级神经网络,由经过训练的多层级神经网络完成多个时刻的预测,每个时刻由所述多层级神经网络输出一个细网格的位移矩阵,直至达到预设的最大推理次数,将多层级神经网络输出的所有时刻细网格的位移矩阵作为待预测布料的位置预测结果；在多层级神经网络每个时刻的预测过程中,将上一时刻细网格的位移矩阵依次与S3得到的各层的插值矩阵相乘,得到各层粗网格的位移矩阵,将上一时刻细网格的位移矩阵以及各层粗网格的位移矩阵输入到经过训练的多层级神经网络中,每个粗网格或者细网格的位移矩阵各自由一个图卷积网络进行处理,得到各个层级的特征矩阵,将各层粗网格的特征矩阵与各自对应的位移矩阵相乘,使各层粗网格的特征矩阵与细网格的特征矩阵维度相同,得到各层粗网格的新的特征矩阵,将各层粗网格的新的特征矩阵与细网格的特征矩阵拼接起来,得到拼接后的特征矩阵,将拼接后的特征矩阵输入给多层感知机,输出下一个时刻细网格的位移矩阵。

### 2.7 资源 & 依赖

**需要什么资源/支持**：高性能显卡

**依赖谁的什么模块**：依赖衣服设计模块；依赖人与衣服交互软件；依赖blender软件；需要三维扫描衣服三维点云坐标模块

### 2.8 可提供的素材

- [ ]  技术架构图 / 流程图
- [ ]  Demo 截图
- [x]  Demo 视频（复现的隐式欧拉法）
- [ ]  效果对比图
- [ ]  实验数据表格
- [ ]  硬件实物照片
- [ ]  系统运行截屏

---

## 三、成员 3：__丁奕翔_（姓名）

### 3.1 基本信息

| 字段 | 内容 |
| --- | --- |
| 研究方向 | 姿态检测 |
| 技术栈（语言/框架/硬件平台） | pytorch |

### 3.2 你负责的模块

**模块名称**：2d姿态检测算法

**一句话说清楚它是干什么的**：从2d图片中计算高精度的人体关键点坐标

**在整个链路中处于什么位置**（勾选）：
- [ ] 输入端：采集用户数据（拍照/扫描/传感器）
- [1 ] 处理层：3D 重建 / 体型建模 / 数字人生成
- [ ] 处理层：服装数字化 / 虚拟试穿 / 渲染
- [ ] 应用层：AI 推荐 / 智能交互 / LLM 对话
- [ ] 展示端：前端界面 / 硬件终端 / AR/VR 展示
- [ ] 基础设施：后端服务 / 部署 / 数据管理

### 3.3 输入和输出

**你的模块需要什么输入？**

- 输入 1：有人存在的图片
- 输入 2：

**你的模块产出什么输出？**

- 输出 1：再图像坐标系下的 人体关键点坐标（胳膊腿位置等）
- 输出 2：

**你的输出会给谁用？**

3d姿态评估/数字人动作驱动

### 3.4 当前成果

**已经做出来的东西**：
1.基于transformer的新视觉基础模型（正在开发
2.在现有2d姿态检测模型上进行的改进
3.

**有没有能跑的 Demo？**
- [ ] 有，可以演示（附截图/视频链接：___）
- [ ] 有原型但还不稳定
- [O ] 目前只有代码/算法，没有可视化的 Demo
- [ ] 还在开发中

**性能指标**：

| 指标 | 数值 |
| --- | --- |
| 精度（如有） | 1-（77 Acc）2-（90-91 PCK，76.2 AP） |
| 速度/帧率 | 1-（比vit慢20%）2-（与hrnet基本一致） |
| 所需设备/显卡 | 1-（2+5090、2.5T+硬盘空间数据集）2-（任意16+显存显卡、~100G+硬盘空间数据集）；；；{但是越多越好} |
| 内存/显存占用 | 1-(32G+显存)2-(~16G显存) |
| 成本 | 不清楚 |

### 3.5 技术亮点 & 差异化

**核心技术/方法**：1-（模型接受高斯信息进行推理）2-（基于hrnet与simcc预测头进行计算加速与精度提升）

**和别人的最大区别**：1-（新的信息输入方法，从像素信息计算转变到高斯信息计算）2-（不知道）

**基于哪些论文/开源项目**：hrnet/Rtmpose/vit/image-gs

### 3.6 专利抢占规划

**原创/创新的技术点**：—

**专利标题设想**：—

**紧迫性**：—
- [ ] 没看到类似的，可以从容准备
- [ ] 有接近的公开工作，需要尽快
- [ ] 非常接近的竞品已存在，必须立即行动

**其他值得占坑的方向**：—

### 3.7 资源 & 依赖

**需要什么资源/支持**：需要尽可能多的算力，算法迭代速度受算力制约

**依赖谁的什么模块**：无/（后期自制数据集可能需要很多人辅助数据标注）

### 3.8 可提供的素材

- [x]  技术架构图 / 流程图
- [x]  Demo 截图
- [ ]  Demo 视频
- [ ]  效果对比图
- [x]  实验数据表格
- [ ]  硬件实物照片
- [ ]  系统运行截屏

---

## 四、成员 4：仇佳伟（姓名）

### 4.1 基本信息

| 字段 | 内容 |
| --- | --- |
| 学校 / 学院 / 年级 | 浙江理工大学/机械工程学院/2025级 |
| 研究方向 | 姿态估计 |
| 技术栈（语言/框架/硬件平台） | python/PyTorch / CUDA  |

### 4.2 你负责的模块

**模块名称**：姿态估计

**一句话说清楚它是干什么的**：就是让计算机从图片或视频里找出人体各个关节和身体姿势

**在整个链路中处于什么位置**（勾选）：
- [ ] 输入端：采集用户数据（拍照/扫描/传感器）
- [√] 处理层：3D 重建 / 体型建模 / 数字人生成
- [ ] 处理层：服装数字化 / 虚拟试穿 / 渲染
- [ ] 应用层：AI 推荐 / 智能交互 / LLM 对话
- [ ] 展示端：前端界面 / 硬件终端 / AR/VR 展示
- [ ] 基础设施：后端服务 / 部署 / 数据管理

### 4.3 输入和输出

**你的模块需要什么输入？**

- 输入 1：多张2D图片
- 输入 2：相机内外参数

**你的模块产出什么输出？**

- 输出 1：2D关键点 3D关键点
- 输出 2：3D关键点可建构3维模型

**你的输出会给谁用？**

服装数字化 

 虚拟试穿

渲染

### 4.4 当前成果

**已经做出来的东西**：
1.复现现有的方法，比如openpose，dhhrnet，smplx等。还没有尝试创新

**有没有能跑的 Demo？**
- [ ] 有，可以演示（附截图/视频链接：___）
- [ ] 有原型但还不稳定
- [√] 目前只有代码/算法，没有可视化的 Demo
- [ ] 还在开发中

**性能指标**： 暂无

| 指标 | 数值 |
| --- | --- |
| 精度（如有） |  |
| 速度/帧率 |  |
| 所需设备/显卡 |  |
| 内存/显存占用 |  |
| 成本 |  |

### 4.5 技术亮点 & 差异化

**核心技术/方法**：通过输入的多张2D图片，提取2D关键点，将多张2D关键点映射到三维坐标系中生成3D关键点

**和别人的最大区别**：暂时没想好

**基于哪些论文/开源项目**：

DensePose: Dense Human Pose Estimation In The Wild

ViTPose: Simple Vision Transformer Baselines for Human Pose Estimation

openpose，vitpose，smplx，dhhrnet，vitpose

### 4.6 专利抢占规划    暂无

**原创/创新的技术点**：

**专利标题设想**：

**紧迫性**：
- [ ] 没看到类似的，可以从容准备
- [ ] 有接近的公开工作，需要尽快
- [√] 非常接近的竞品已存在，必须立即行动

**其他值得占坑的方向**：

### 4.7 资源 & 依赖   暂无

**需要什么资源/支持**：

**依赖谁的什么模块**：

### 4.8 可提供的素材

- [ ]  技术架构图 / 流程图
- [ ]  Demo 截图
- [ ]  Demo 视频
- [ ]  效果对比图
- [ ]  实验数据表格
- [ ]  硬件实物照片
- [x]  系统运行截屏

---

## 五、成员 5：__刘鹏亮_（姓名）

### 5.1 基本信息

| 字段 | 内容 |
| --- | --- |
| 研究方向 | 魔镜人像采集及三维数字人显示 |
| 技术栈（语言/框架/硬件平台） | 多相机联合标定及数字人智能交互 |

### 5.2 你负责的模块

**模块名称**：数据采集及数字人显示交互

**一句话说清楚它是干什么的**：

**在整个链路中处于什么位置**（勾选）：
- [x] 输入端：采集用户数据（拍照/扫描/传感器）
- [ ] 处理层：3D 重建 / 体型建模 / 数字人生成
- [ ] 处理层：服装数字化 / 虚拟试穿 / 渲染
- [x] 应用层：AI 推荐 / 智能交互 / LLM 对话
- [x] 展示端：前端界面 / 硬件终端 / AR/VR 展示
- [ ] 基础设施：后端服务 / 部署 / 数据管理

### 5.3 输入和输出

**你的模块需要什么输入？**

- 输入 1:数字人三维模型
- 输入 2:人物模特

**你的模块产出什么输出？**

- 输出 1：三维重建数据集
- 输出 2：显示数字人模型

**你的输出会给谁用？**

陈宇轩-3dgs模型

### 5.4 当前成果

**已经做出来的东西**：
1.相机矩阵采集数据
2.人物模型显示终端
3.智能交互-ai agent

**有没有能跑的 Demo？**
- [ ] 有，可以演示（附截图/视频链接：___）
- [x] 有原型但还不稳定
- [ ] 目前只有代码/算法，没有可视化的 Demo
- [ ] 还在开发中

**性能指标**：

| 指标 | 数值 |
| --- | --- |
| 精度（如有） |  |
| 速度/帧率 |  |
| 所需设备/显卡 | 43寸显示器/30系列及以上显卡用以跑LLM |
| 内存/显存占用 | 16G显存，20G内存，磁盘20G以上 |
| 成本 | 暂定 |

### 5.5 技术亮点 & 差异化

**核心技术/方法**：

1. 高精度多相机联合空间标定与同步数据采集算法，保障三维重建数据的空间一致性
2. 基于本地化编排的数字人 AI Agent 交互架构，结合复杂的状态管理与记忆持久化机制，实现低延迟、具备上下文感知的多轮智能对话

**和别人的最大区别**：

1. **全链路的本地化闭环**：打通了从“多视角硬件采集”到“基于工具调用（Tool-Calling）的本地化 AI Agent 交互”的链路，避免了纯云端方案的高延迟和数据隐私风险
2. **深度的 Agent 交互能力**：市面上的数字人多为无状态的单次问答，本模块通过引入记忆持久化与状态隔离，使“魔镜”数字人能够进行有记忆、有个性化的深度交互，真正承担起应用层的智能导引与推荐作用。

**基于哪些论文/开源项目**：

1. 多相机标定与视觉处理：OpenCV 相关开源算法。
2. 模型部署与交互调度：Ollama 容器化部署生态、DeepSeek-R1 等开源大语言模型、Dify 等复杂应用编排框架。

### 5.6 专利抢占规划

**原创/创新的技术点**：

- **特定的硬件采集系统与数据集生成架构**：针对“魔镜”特殊形态设计的相机阵列排布、联合标定方法，以及无缝对接 3DGS 模型训练的数据集自动化生产管线。
- **融合状态持久化的数字人终端调度方法**：一种在显存与内存受限的显示终端上，动态分配视觉采集、显示渲染与本地 LLM 推理资源，并维持交互状态不丢失的机制。

**专利标题设想**：

- 《一种基于多相机联合标定的魔镜人像采集及三维数据集构建系统》
- 《一种基于状态持久化与本地编排的数字人智能交互系统及终端设备》

**紧迫性**：
- [ ] 没看到类似的，可以从容准备
- [x] 有接近的公开工作，需要尽快
- [ ] 非常接近的竞品已存在，必须立即行动

**其他值得占坑的方向**：

一种用于三维数字人生成前置阶段的模特姿态与相机矩阵实时校验方法。

### 5.7 资源 & 依赖

**需要什么资源/支持**：

- **硬件**：需要落实“魔镜”实体硬件（43寸及以上高透显式屏幕与外框）、多相机阵列（含支架与硬件同步触发线缆），以及用于本地部署 LLM 和渲染的高性能主机（至少配备 RTX 30 系列及以上显卡，16G+ 显存，20G+ 内存）。
- **场地**：需要一处光照环境相对稳定、空间充裕的场地，用于长期架设多相机矩阵并进行联合空间标定；前期需要若干真人模特配合进行打光测试与动作采集验证。
- **网络与环境配置**：由于涉及本地化 Agent 编排与大模型（如 DeepSeek-R1、Ollama 容器）下载部署，初期可能需要无限制的外网访问权限以及充足的磁盘存储空间（>20G 仅为基础，长期采集建议预留更大 SSD 存储）。

**依赖谁的什么模块**：

高度依赖【陈宇轩】的【3DGS 模型生成与渲染模块】:本模块负责的前端显示与交互，其视觉核心就是陈宇轩产出的数字人三维模型。3DGS 模型生成的精度、加载速度以及与交互逻辑对接的 API 延迟，将直接决定“魔镜”最终的展示效果和流畅度。

### 5.8 可提供的素材

- [x]  技术架构图 / 流程图
- [x]  Demo 截图
- [ ]  Demo 视频
- [ ]  效果对比图
- [ ]  实验数据表格
- [x]  硬件实物照片
- [ ]  系统运行截屏

---

## 五、成员 5：__叶开_（姓名）

### 5.1 基本信息

| 字段 | 内容 |
| --- | --- |
| 研究方向 | 目标分割 |
| 技术栈（语言/框架/硬件平台） | python/PyTorch / CUDA  |

**模块名称**：图片人物及部位分割

**一句话说清楚它是干什么的**：

**在整个链路中处于什么位置**（勾选）：
- [ ] 输入端：采集用户数据（拍照/扫描/传感器）
- [√] 处理层：3D 重建 / 体型建模 / 数字人生成
- [ ] 处理层：服装数字化 / 虚拟试穿 / 渲染
- [ ] 应用层：AI 推荐 / 智能交互 / LLM 对话
- [ ] 展示端：前端界面 / 硬件终端 / AR/VR 展示
- [ ] 基础设施：后端服务 / 部署 / 数据管理

### 5.3 输入和输出

**你的模块需要什么输入？**

- 输入 1: 相机拍摄照片

**你的模块产出什么输出？**

- 输出 1：人物与背景的分割
- 输出 2：人物的各个部位（衣物）的单独分割

**你的输出会给谁用？**

用于生成人体的3D建模

### 5.4 当前成果

**已经做出来的东西**：
实现人与“简单背景”的分割

实现人的各部位关节以及衣物的简单分割（目前存在缺陷）

**有没有能跑的 Demo？**
- [ ] 有，可以演示（附截图/视频链接：___）
- [√] 有原型但还不稳定
- [ ] 目前只有代码/算法，没有可视化的 Demo
- [ ] 还在开发中

**性能指标**：

| 指标 | 数值 |
| --- | --- |
| 精度（如有） |  |
| 速度/帧率 |  |
| 所需设备/显卡 | 5060ti |
| 内存/显存占用 | 32g运存/16g显存 |
| 成本 |  |

### 5.5 技术亮点 & 差异化

**核心技术/方法**：

**和别人的最大区别**：

**基于哪些论文/开源项目**：

Few-Shot Semantic Segmentation Meets SAM3

Self-Correction for Human Parsing

### 5.6 专利抢占规划

**原创/创新的技术点**：

**专利标题设想**：

**紧迫性**：
- [ ] 没看到类似的，可以从容准备
- [ ] 有接近的公开工作，需要尽快
- [ √] 非常接近的竞品已存在，必须立即行动

**其他值得占坑的方向**：

### 5.7 资源 & 依赖

**需要什么资源/支持**：

**依赖谁的什么模块**：

### 5.8 可提供的素材

- [ ]  技术架构图 / 流程图
- [x]  Demo 截图
- [ ]  Demo 视频
- [x]  效果对比图
- [ ]  实验数据表格
- [ ]  硬件实物照片
- [x]  系统运行截屏

---

### 不够填复制前面的个人范本！

---

## 六、系统级专利 & IP 规划（所有人一起讨论）

### 6.1 系统级专利（不属于单个模块，而是整条链路的）

> 思考：把我们所有模块串起来形成的完整系统，本身是不是一个可以申请专利的创新？
> 

**系统级专利候选**：

| 专利方向 | 大致描述 | 负责人 | 紧迫性 |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |

### 6.2 我们最需要防守的技术点

> 如果只能申请 3 个专利，最应该保护哪 3 个技术点？（所有人投票）
> 
1. 
2. 
3. 

### 6.3 竞品专利扫描

> 是否了解竞品已申请的相关专利？列出来，避免踩坑。
> 

---

## 七、整合检查（负责人在收齐信息后填）

### 7.1 链路完整性

| 环节 | 是否有模块覆盖 | 负责人 | 缺口/风险 |
| --- | --- | --- | --- |
| 用户体型获取 |  |  |  |
| 3D 人体重建 |  |  |  |
| 服装数字化 |  |  |  |
| 虚拟试穿渲染 |  |  |  |
| 智能推荐/交互 |  |  |  |
| 前端/硬件展示 |  |  |  |

### 7.2 模块接口对齐

| 上游模块 → 下游模块 | 输出格式 | 输入格式 | 是否对齐 |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |

### 7.3 Demo 可演示性评估

| 模块 | 能否在 PPT 中演示 | 演示形式 | 预计准备时间 |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |

---

> **填写提示**
1. 不用写得正式，关键信息完整就行，后续由我统一包装
2. **截图/视频 > 一切文字描述**——投资人看效果
3. 输入输出那栏必须认真填——这决定了模块间怎么串成产品
4. 专利不是”有没有写过”，是”应该写什么来保护我们的创新”
5. 不确定的地方写”待定”或”?“，不要空着
> 

[business_plan_v2](https://www.notion.so/business_plan_v2-34bfef7b220e80a3be2bf6b710c93d53?pvs=21)